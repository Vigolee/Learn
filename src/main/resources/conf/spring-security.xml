<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security.xsd">

    <http auto-config="true">
        <intercept-url pattern="/security/admin" access="hasRole('USER')" />
        <!-- access denied page 权限不够跳转 -->
        <access-denied-handler error-page="/security/403" />
        <!-- 自定义登陆界面-->
        <form-login login-page="/security/login" default-target-url="/security/"
                    login-processing-url="/security/j_spring_security_check"
                    authentication-failure-url="/security/login?error" username-parameter="username"
                    password-parameter="password" />
        <!--<logout logout-success-url="/login?logout" />-->
        <csrf />
    </http>

    <!-- Select users and user_roles from database -->
    <authentication-manager>
        <authentication-provider>
            <!-- 基于内存验证(角色: User) -->
            <user-service>
                <user name="admin" password="admin" authorities="ROLE_USER" />
                <user name="admin2" password="admin2" authorities="ROLE_USER2" />
            </user-service>
        </authentication-provider>
        <authentication-provider>
            <!-- 数据库验证-->
            <jdbc-user-service data-source-ref="dataSource"
                               users-by-username-query="select username, password, enable from users where username=?"
                               authorities-by-username-query="select username, role from user_roles where username=?  " />
        </authentication-provider>
    </authentication-manager>

</beans:beans>